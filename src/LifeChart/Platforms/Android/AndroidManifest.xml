<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Benachrichtigungen (Android 13+) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Exakte Alarme (Android 12+) -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />

    <!-- DND-Bypass -->
    <uses-permission android:name="android.permission.ACCESS_NOTIFICATION_POLICY" />

    <!-- Alarme nach Neustart wiederherstellen -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- Akkuoptimierung deaktivieren -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <application android:label="LifeChart" android:allowBackup="true" android:dataExtractionRules="@xml/extraction_rules">

        <receiver
            android:name=".Receivers.MedicationAlarmReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="lifechart.MEDICATION_ALARM" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".Receivers.BootReceiver"
            android:exported="true">
            <intent-filter
                android:permission="android.permission.RECEIVE_BOOT_COMPLETED">
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.LOCKED_BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

    </application>
</manifest>
